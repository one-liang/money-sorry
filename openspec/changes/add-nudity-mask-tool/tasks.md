## 1. 資產準備

- [x] 1.1 下載 MoveNet SinglePose Thunder v4 的 `model.json` 與三個權重 shard，放進 `assets/model/`（來源：Kaggle Models `google/movenet/frameworks/tfJs/variations/singlepose-thunder/versions/4`，需帶 `?tfjs-format=file&tfhub-redirect=true`）
- [x] 1.2 取得 `@tensorflow/tfjs` 4.22.0 的 `dist/tf.min.js`，放進 `assets/vendor/`
- [x] 1.3 寫一個 `build-censor.js`：把 `tf.min.js`、`modelTopology`、`weightSpecs`、權重 base64 內嵌進 `censor.html`，並在 README 記錄「換模型時重跑這支」的步驟
- [x] 1.4 確認 `.gitignore` 已排除 `test/`；`assets/model/`（12MB，鮮少變動）進版控以維持離線可重建，`censor.html` 與 `tools/verify/out/` 為產物不進版控

## 2. 執行骨架與模型載入

- [x] 2.1 建立 `censor.html` 骨架：檔案選取（multiple + 拖放）、清單、預覽區
- [x] 2.2 以 `tf.io.fromMemory({modelTopology, weightSpecs, weightData})` 從內嵌 base64 載入模型，設定 backend 為 `webgl`
- [x] 2.3 加入零網路自我檢查：包裝 `fetch` / `XMLHttpRequest.open` / `WebSocket`，任一被呼叫即在介面上明確報錯
- [x] 2.4 加入 backend 檢查：非 `webgl` 時顯示警告與預估耗時，不靜默降速
- [x] 2.5 以 `file://` 雙擊開啟驗證：模型可用、零網路呼叫、單張推論落在數十毫秒級

## 3. 偵測與座標還原

- [x] 3.1 實作前處理：置中 pad 成正方形 → resize 256×256 → `int32`（不做正規化）
- [x] 3.2 實作關鍵點座標還原：扣除 pad 位移換算回原圖像素座標
- [x] 3.3 以 `test/` 中 302×800、800×520、1000×1300 三種長寬比驗證關鍵點確實落在原圖範圍內（前處理改走 tf 張量路徑；canvas drawImage 的降採樣濾波不同，實測會讓 2 張圖的分級改變）
- [x] 3.4 實作逐張序列處理與資源釋放（ImageBitmap / tensor / object URL）

## 4. 結構合理性檢查

- [x] 4.1 實作肩部信心閘門（`min(左肩,右肩) ≥ 0.25`）
- [x] 4.2 實作關鍵點塌陷檢查（有效點包圍盒面積 / 圖片面積 `≥ 4%`，有效點數 `≥ 4`）
- [x] 4.3 實作軀幹比例檢查（`軀幹長/肩寬 ∈ [0.55, 3.2]`、`髖寬/肩寬 ≤ 1.8`）
- [x] 4.4 實作半身模式錨點檢查（無髖部時要求臉部信心 `≥ 0.30` 且肩部信心 `≥ 0.40`）
- [x] 4.5 以 `test/` 驗證這 6 張確實被擋下：`013460_21`、`013460_01`、`177657_02`、`019307_04`、`11554_10`、`10005_0`；且 `10003_0`（半身但有臉）確實通過。另有 `11554_06` 因卡在門檻邊界（bust 肩部 0.40）也被擋下，共 7 張

## 5. 遮罩幾何

- [x] 5.1 實作身體軸（肩中點→髖中點）與垂直軸；半身模式退回畫面垂直
- [x] 5.2 實作正／背／側三分判定
- [x] 5.3 實作胸部帶（`t ∈ [0.30,1.15]`、半寬 `0.72`）
- [x] 5.4 實作下半身帶三種變體：正面胯部帶、背面臀部帶（`×1.25`）、側面合併帶（`×1.55`）
- [x] 5.5 實作 M字腿判定與膝距定寬（`0.75` 係數、`2.2` 上限、`t ∈ [1.45,2.70]`）
- [x] 5.6 實作旋轉矩形 → 外接軸對齊矩形 → 外擴 12% → clamp 到圖片邊界
- [x] 5.7 遮罩以原圖像素座標儲存；繪製時四角走與照片相同的仿射變換，不經中間畫布

## 6. 複核介面

- [x] 6.1 實作分級（🟢/🟡/🔴）與縮圖網格，每張疊上遮罩建議
- [x] 6.2 實作單張放大檢視與鍵盤流（Enter 確認並跳下一張未處理）
- [x] 6.3 實作遮罩的拖曳新增
- [x] 6.4 實作遮罩的移動與縮放（含控制點）
- [x] 6.5 實作遮罩的刪除；一張可以沒有任何遮罩並仍標記為已處理
- [x] 6.6 實作「尚有 N 張未處理」提示與輸出鍵鎖死

## 7. 輸出

- [x] 7.1 實作已遮版繪製：白底 → 照片 → 黑色遮罩，維持來源尺寸與長寬比，不裁切不套框
- [x] 7.2 實作原檔版：直接取 `File` 的位元組寫入，不經 canvas、不重新編碼
- [x] 7.3 實作 store-only ZIP（含 CRC32、local header、central directory、EOCD、UTF-8 flag bit 11），輸出 `masked/` 與 `raw/` 兩個目錄
- [x] 7.4 實作同名檔案自動加序號
- [x] 7.5 多張只觸發一次下載；單張時的行為與多張一致（仍需兩個版本，故一律打包）

## 8. 驗收

- [x] 8.1 把 spike 的驗證腳本整理進 `tools/verify/`（跑完 `test/` 產生 overlay 與判讀表），作為日後擴充素材時的回歸工具
- [x] 8.2 以 `test/` 31 張重跑並人工判讀 —— 實測 🟢 23 / 🟡 1 / 🔴 7；抽驗 8 張疊圖，7 張遮罩位置正確、1 張（`10003_0` 左乳邊緣）需微調、**危險漏遮 0**
- [x] 8.3 ZIP 以系統 `unzip -t` 與 macOS `ditto -xk` 外部驗證；抽驗 `raw/` 內檔案的雜湊與來源檔一致
- [x] 8.4 端到端計時 —— 推論中位數 18–21 ms/張，100 張約 2 秒，與設計估算相符
- [x] 8.5 確認 `index.html` 位元組未變、`openspec/specs/photo-framing/spec.md` 未變

## 10. 以人工遮罩成品驗收（追加）

- [x] 10.1 把使用者提供的兩張人工遮罩成品放進 `test/reference/`，以 Playwright 量出其中的純黑矩形
- [x] 10.2 建立 `tools/verify/censor.spec.js`：參考成品比對（100% 覆蓋 + 面積上限）、裸露區域覆蓋、全批不變式，共 16 個測試
- [x] 10.3 建立 `tools/verify/targets.json`：12 張圖的裸露區域人工標定，作為「沒有漏遮」的可重跑證據
- [x] 10.4 修正比對揪出的缺陷 —— 側面分類無鑑別力（移除）、半寬上限套用順序錯誤、下半身帶未錨定髖部（實測漏遮 35%）、兩帶之間有縫隙、趴跪姿臀部補不滿（改為擋下）
- [x] 10.5 修正 ZIP 時間戳寫死為 1980-01-01，改為輸出當下時間；`verify-output.js` 加上時間戳斷言
- [x] 10.6 全部驗證通過：Playwright 16/16、`verify.js` 與基準一致、`verify-output.js` 全過

## 9. 文件

- [x] 9.1 更新 `README.md`：新增 `censor.html` 的使用方式、兩個工具各自獨立的說明、換模型步驟
- [x] 9.2 在 README 明示已知限制：一張圖多尊 figure 時只會偵測到一尊（目前唯一已知的靜默漏遮路徑）、側面姿勢一律轉人工、`censor.html` 約 18MB 開啟需數秒
