## 1. 共用模組抽取（可獨立回退）

- [x] 1.1 建立 `lib/zip.js`：從 `censor.html` 搬出 `CRC_TABLE` / `crc32` / `dosDateTime` / `makeZip` / `dedupe` / `download`，以 classic script 掛在單一全域命名空間下，不使用 ES module
- [x] 1.2 `makeZip` 支援 ZIP 內的資料夾路徑（entry name 帶 `<資料夾>/` 前綴），確認 `unzip -t` 與 Windows 檔案總管皆能正確解開
- [x] 1.3 `censor.html` 改為 `<script src="./lib/zip.js">` 並刪除頁內重複實作
- [x] 1.4 跑 `node tools/verify/censor.mjs`，並比對抽換前後對同一批照片的輸出位元組完全相同；若未通過則還原 1.3，改由 `studio.html` 自帶一份

## 2. `studio.html` 骨架

- [x] 2.1 建立 `studio.html`，沿用既有兩頁的 CSS 變數（色系、字級級距）與左清單／右預覽雙欄佈局
- [x] 2.2 加入 `file://` 防呆告示（比照 `censor.html` 的 `#protocol-gate`）
- [x] 2.3 實作選檔與拖放區、清單渲染（縮圖、檔名、大小、尺寸、badge），此階段先不接遮罩
- [x] 2.4 建立單一 item 資料結構，同時容納 `file` / `measure` / `adjust` / `boxes` / `areaPct` / `frameOn` / `censorOn` / `status`
- [x] 2.5 無法解碼的檔案在加入清單時即標示失敗並附原因（含 Chrome 的 HEIC 提示），不進入輸出

## 3. 開關與一鍵

- [x] 3.1 每列加入套框與遮罩兩個晶片，亮／暗表示開關狀態，套框預設開、遮罩預設關
- [x] 3.2 點擊晶片只切換該開關，不觸發切換預覽；點列身才切換預覽
- [x] 3.3 清單上方為套框與遮罩各加一組「全開／全關」，附即時計數與分段點亮，兩組互不牽動；「遮罩全關」另附復原
- [x] 3.4 輸出按鈕文字顯示總張數與含遮罩張數（只計實際有遮罩框的，零命中的另行常駐警示）

## 4. 合成管線與輸出矩陣

- [x] 4.1 移植 `contentRect` / `fitScale` / `panRange` / `srcRect` 與外框 data URI
- [x] 4.2 實作遮罩框的座標換算 `((box.x - sx) * f, (box.y - sy) * f)`，並與 `srcRect` 共用同一組參數
- [x] 4.3 實作單次合成：白底 → 照片 → 遮罩 → 外框 → 一次 `toBlob('image/jpeg', 0.92)`
- [x] 4.4 實作「套框關、遮罩開」路徑：原尺寸畫布 ＋ 原座標遮罩
- [x] 4.5 實作「兩者皆關」路徑：直接取用來源 `File` 的位元組，不解碼、不重新編碼，副檔名跟隨原圖
- [x] 4.6 清單上為「兩者皆關」的項目加上「原圖原樣」標示

## 5. 雙模式預覽

- [x] 5.1 預覽區改為三層：`<img id=pphoto>` / `<canvas id=pboxes>` / `<img id=pframe>`，由單一 `layout()` 寫入同一組百分比幾何
- [x] 5.2 遮罩關閉時，預覽與互動行為（拖曳平移、滾輪／捏合縮放、雙擊重置、邊界夾制）與 `index.html` 一致，且不顯示模式切換
- [x] 5.3 加入模式切換控制項，僅在遮罩開啟時出現，預設為「編輯遮罩」
- [x] 5.4 「編輯遮罩」模式：顯示完整未裁切原圖、外框淡出，並以虛線疊上裁切範圍
- [x] 5.5 「編輯遮罩」模式的框互動（拖曳新增、點選、八向縮放把手、Delete 刪除、推算框虛線標示）自 `censor.html` 移植
- [x] 5.6 「調整構圖」模式：顯示 1000×1000 裁切結果、外框固定疊上，遮罩唯讀且隨照片變換
- [x] 5.7 確認在裁切範圍外的遮罩框於構圖模式下被畫布邊界截斷，但儲存座標不變、切回編輯模式仍完整

## 6. 輸出命名與序號

- [x] 6.1 加入輸出命名欄位與消毒邏輯（移除 `/ \ : * ? " < > |` 與前後空白句點，保留中文）
- [x] 6.2 命名結果即時標在清單每一列上（輸出檔名 ＋ 最左邊的序號），不另設集中預覽區
- [x] 6.3 實作序號：位數依可輸出總張數決定，失敗項不佔號、連號重排
- [x] 6.4 ZIP 一律開一層資料夾；未命名時沿用 `money-sorry-<YYYYMMDD>-<HHMM>` 作為 ZIP 與資料夾名
- [x] 6.5 單張直接下載該檔案，不打包也不產生資料夾

## 7. 排序、追加與清空

- [x] 7.1 每列加入序號（純文字讀數）與獨立的拖曳把手，以 pointer 事件（不用 pointer capture）實作排序並加上 FLIP 動畫，只有把手可觸發
- [x] 7.2 排序後即時重編序號並更新清單顯示
- [x] 7.3 排序後維持原本預覽的那張照片為選取狀態，不因索引變動而跳張
- [x] 7.4 提供鍵盤替代（把手聚焦時上下移動項目）
- [x] 7.5 選檔與拖入改為追加，既有項目的 `boxes` / `adjust` / `measure` 與目前選取一律不變
- [x] 7.6 `revokeObjectURL` 只在清空時執行，追加時不釋放既有資源
- [x] 7.7 加入「清空 (N)」動作：次要視覺權重、按下需確認（附取消按鈕與 Esc，不逾時）、確認後釋放全部資源
- [x] 7.8 以 `name + size + lastModified` 判定重複並在該列標示，不阻擋、不自動移除
- [x] 7.9 驗證從桌面拖入外部檔案不會被誤判為內部排序

## 8. 遮罩整合

- [x] 8.1 移植偵測管線（前處理、雙解析度推論、NMS 合併、框膨脹、幾何補框）與模型載入進度回饋
- [x] 8.2 改為延遲載入：首次有任一張啟用遮罩時才下載模型與 runtime
- [x] 8.3 模型載入失敗時所有遮罩開關退回關閉並顯示明確錯誤，不得表現為「未偵測到任何部位」
- [x] 8.4 `maskedRatio` 的分母改為實際輸出的可見區域；套框開啟時只計入落在裁切範圍內的遮罩面積
- [x] 8.5 清單警示（未偵測／遮蓋過多／含推算）沿用 `censor.html` 的三種標示

## 9. 導覽與文件

- [x] 9.1 三頁導覽列同步更新為三個項目，`studio.html` 排第一並標示為建議入口
- [x] 9.2 `index.html` 與 `censor.html` 的副標各加一句指向新頁（僅改文案，不動邏輯）
- [x] 9.3 `studio.html` 加入使用說明摺疊區（兩個開關的四種組合、雙模式、命名規則、排序與清空）
- [x] 9.4 更新 `README.md`：三工具對照表、新頁使用方式、`lib/` 與檔案結構、「先遮後套」改註明僅適用於分頁流程
- [x] 9.5 決定導覽列第三個 tab 的中文標題（design.md Open Questions）

## 10. 驗證

- [x] 10.1 建立 `tools/verify/studio.mjs` 骨架，沿用 `preview-adjust.mjs` 的基準頁面比對手法
- [x] 10.2 釘住不變量一：套框開、遮罩關的輸出與 `index.html` 同組調整的輸出 SHA-256 相同
- [x] 10.3 釘住不變量二：遮罩開、套框關的輸出與 `censor.html` 同組框的輸出 SHA-256 相同
- [x] 10.4 釘住不變量三：兩者皆關時，ZIP 內的位元組與來源檔案完全相同
- [x] 10.5 兩者皆開時，以預覽截圖與實際輸出 JPEG 做像素比對，確認所見即所得
- [x] 10.6 驗證命名與序號：消毒、補零、失敗不佔號、ZIP 內資料夾層級、中文不亂碼
- [x] 10.7 驗證追加不重置、清空需確認、排序後序號與選取狀態正確
- [x] 10.8 驗證只套框的整批流程全程不下載模型與 runtime
- [x] 10.9 收尾提示人工確認遮罩位置（Playwright 驗得了有沒有畫出方塊，驗不了遮的位置對不對）
