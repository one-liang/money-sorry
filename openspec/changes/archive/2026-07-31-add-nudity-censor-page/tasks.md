## 1. 資產與執行環境

- [x] 1.1 取得 NudeNet detector ONNX（320n，約 11.6MB）放入 `models/`
- [x] 1.2 取得 onnxruntime-web（`ort.min.js` 與對應 `.wasm`）放入 `vendor/`，並以 `ort.env.wasm.wasmPaths` 指向本地路徑
- [x] 1.3 新增 `serve.cmd`（`npx serve .`），確認 `.wasm` 以正確 MIME type 供應
- [x] 1.4 決定 `models/` 是否進版控並更新 `.gitignore`（design.md Open Question 1）
- [x] 1.5 `censor.html` 偵測自身 `location.protocol === 'file:'` 時顯示指引，不讓模型載入靜默失敗

## 2. 偵測管線

- [x] 2.1 模型載入與 warm-up，載入期間介面顯示狀態；載入失敗顯示明確錯誤（不得表現為「0 個部位」）
- [x] 2.2 前處理：letterbox 縮放至 320×320、正規化、NCHW，並保留還原原圖座標所需的 scale/offset
- [x] 2.3 後處理：解析輸出、套用信心閾值 **0.15**（解碼與 NMS 兩處同值，見 design D8）、逐類 NMS、座標還原至原圖像素
- [x] 2.4 過濾出應遮類別（9 類，`EXPOSED` 與 `COVERED` 同等對待）
- [x] 2.5 實測瀏覽器 WASM 單張推論耗時，據此定案解析度組合（`[320]` / `[320,640]` / `[320,480,640]`）
- [x] 2.6 逐張序列處理並即時釋放 ImageBitmap 與 tensor，避免批次時記憶體尖峰

## 3. 遮罩框後處理

- [x] 3.1 跨解析度合併：以 NMS 保留最高分框（IoU 門檻約 0.45），**不得**使用外接聯集
- [x] 3.2 `EXPOSED`/`COVERED` 正規化為同一部位鍵，避免同處疊兩框
- [x] 3.3 框向外膨脹固定比例（初值 12~15%），並夾在圖片邊界內
- [x] 3.4 幾何補框：偵測到兩個胸部框且無任何私處框時，依兩胸中點與間距推算胯部框，標記 `inferred: true`
- [x] 3.5 單框面積上限檢查，超過者標示為可疑但仍保留
- [x] 3.6 驗證補框位置：以 `10002_0` / `10004_0` / `10006_0`（實測漏遮下體的三張）確認補框有蓋到目標
- [x] 3.7 量測補框的實際誤遮率（靜態推估約 13 張觸發、僅 4~5 張真正需要）；若位置偏差過大則改用 Pose 關鍵點（design Open Question 3）

## 4. 護欄與警示

- [x] 4.1 零命中偵測：整張無框時於清單標示警示（涵蓋真陰性與漏遮兩種情況，不自動區分）
- [x] 4.2 遮罩面積佔比計算（需處理框重疊，避免重複計入面積）
- [x] 4.3 面積超過門檻（初值 35%）時於清單標示「遮後可能不堪用」
- [x] 4.4 以 `LTD-FIG-11554_04`（實測零命中）驗證 4.1、以 `10003_0`（實測遮 91.4%）驗證 4.3

## 5. 介面

- [x] 5.1 沿用 `index.html` 的 CSS 變數與版面骨架（cream/amber/brown、雙欄、圓角面板）
- [x] 5.2 拖放與檔案選擇區、可點選的左側清單（含縮圖、檔名、警示標示）
- [x] 5.3 預覽區顯示遮罩後結果；非同步偵測需加 token 競態防護，確保顯示的是最後選取的那一張
- [x] 5.4 清單支援 ↑↓ 鍵切換（比照 `index.html` 既有行為）
- [x] 5.5 進度顯示與完成訊息
- [x] 5.6 頁尾說明：本工具遮的是解剖部位、不保證通過任何平台審核、遮罩需在套框之前

## 6. 人工調整

- [x] 6.1 預覽區改為可互動畫布，繪製既有方塊並顯示可選取狀態
- [x] 6.2 拖曳空白處新增方塊
- [x] 6.3 點選既有方塊後可刪除（Delete/Backspace）與拖曳移動
- [x] 6.4 推算補框以不同視覺樣式標示，與偵測框可區分
- [x] 6.5 每張的調整結果獨立保存，切換預覽對象再切回不遺失
- [x] 6.6 刪除過的推算補框不得在重新預覽時自動復原

## 7. 輸出

- [x] 7.1 以原始尺寸輸出，不裁切不縮放
- [x] 7.2 檔名規則（沿用 `<原檔名>-censored.jpg` 之類的後綴慣例，同名自動去重）
- [x] 7.3 移植 `index.html` 的手寫 store-only ZIP 實作（多張打包成單一 ZIP 只觸發一次下載，單張直接下載不包 ZIP）
- [x] 7.5 ZIP 驗證：`unzip -t` 無錯誤、中文檔名不亂碼（UTF-8 + flag bit 11）、同名檔案自動去重
- [x] 7.4 部分失敗仍照常輸出成功的部分，並標示失敗筆數與原因

## 8. 驗證

- [x] 8.1 `tools/verify/` 新增 Playwright 腳本：起 server、載入 `censor.html`、依序丟入 `test/` 的 31 張、匯出遮罩結果與偵測框 JSON
- [x] 8.2 Playwright 驗證互動：手動新增／刪除／移動方塊、切換預覽後調整不遺失
- [x] 8.3 Playwright 驗證護欄：零命中與面積警示確實出現在清單
- [x] 8.4 把 31 張輸出拼成 contact sheet，逐張人工／視覺判讀，分類為「完整遮蔽／部分遮蔽／漏遮／誤遮」
- [x] 8.5 與 spike 基準比較：張級召回應 ≥ 29/31，部位級完整正確應**高於** spike 的 65%（幾何補框與降閾值的預期效益）
- [x] 8.6 產出評估報告，列出仍然失敗的張數與型態
- [x] 8.7 確認 `index.html` 雙擊開啟後行為與變更前一致（無回歸）

## 9. 文件

- [x] 9.1 README 新增遮罩工具章節，明確區分「`index.html` 雙擊即用」與「`censor.html` 需本機 server」
- [x] 9.2 README 說明遮罩須在套框之前的順序限制與原因
- [x] 9.3 README 記錄實測命中率與已知失敗型態，不對審核結果做任何承諾
- [x] 9.4 更新檔案結構圖
